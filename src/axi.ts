// there is one array [1,1,2,2,3,3,4,5]

// const distinctElements = (number: []): number[] => {
//   const result: number[] = [];
//   const map = {};

//   for (let index = 0; index < number.length - 1; index++) {
//     const element = number[index];

//     if (!map[element]) {
//       map[element] = element;
//     }
//   }
// };

// "rizwanrizwantest123"
//

// const largestSubsatring = (
//   input: string,
//   strt: number,
//   output: string
// ): string => {
//   let result = "";
//   const element = input[strt];
//   const nextIndices = input.indexOf(element);
//   const substring = input.substring(strt, nextIndices);
//   result = result;
//   if (substring === largestSubsatring(input, strt + 1, result))
//     //   for (let i = 0; i < input.length; i++) {
//     //     const element = input[i];
//     //     const nextIndices = input.indexOf(element);

//     //     if (nextIndices !== -1) {
//     //       const substring = input.substring(i, nextIndices);

//     //       if (substring === input.substring(nextIndices, substring.length)) {
//     //         result = substring;
//     //       }
//     //     }
//     //   }

//     // largestSubsatring();

//     return result;
// };

// console.log(largestSubsatring("rizwanrizwantest123"));

// limit number of

//3:30:
// 3:31
// 33:32

// const cahce = {1:}
